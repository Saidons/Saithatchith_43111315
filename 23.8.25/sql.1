create database Workers;
show databases;
use Workers;
create table workers(
Workername Varchar(50) not null,
Worker_id int not null,
mobilenumber int not null,
workerposition Varchar(10)
);
insert into workers (Workername,Worker_id,mobilenumber,workerposition) values
('gopi',1,562812,'employe');
select * from workers;
insert into workers (Workername,Worker_id,mobilenumber,workerposition) values
('sai',3,663388,'CEO');
alter table workers
drop index idx_mobile;
create index idx_mobile on workers(mobilenumber);


SHOW INDEXES FROM workers;
alter table workers 
modify Worker_id int;

ALTER TABLE workers 
ADD CONSTRAINT unique_worker UNIQUE (Worker_id);

alter table workers
drop index unique_worker;

show indexes from workers;
describe workers;

#constraints examples


create database person;
show databases;
create table persons(
personname Varchar(40)not null,
personid int auto_increment primary key,
personage int check (personage>18 and personage<120)
);
insert into persons(personname,personage) values
('sai',23);
 alter table persons
 drop personage;
 select*from persons;
 alter table persons
 add personage int;
 select*from persons;
alter table persons
add constraint chk_1 check (personage>18 and personage<100);
insert into persons(personname,personage) values
('deva',48);
select distinct personname from persons;
delete from persons 
where personid=1;

#foreign key constraints

create database category
use category;
create table categories(
cid int not null primary key,
cname varchar(30) not null,
cdes varchar(50) not null
);
insert into categories (cid,cname,cdes) values
(003,'coolers','good');
insert into categories (cid,cname,cdes) values
(004,'watches','very good');
create table products(
pid int not null primary key,
pname varchar(30) not null,
pdes varchar(40) not null,
cid int not null,
foreign key (cid) references  categories (cid)
on update cascade
);
insert into products (pid,pname,pdes,cid) values
(597,'iphone','Works good',004);
select* from products;
select * from categories;
update categories 
set cid = 007

where cid = 003;
