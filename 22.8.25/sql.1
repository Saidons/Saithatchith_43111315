show databases;
create database sathyabama;
use sathyabama;
show tables;
create table cse(
sid int,
sname varchar(30),
srno int,
smarks int
);
show tables;
show tables from sathyabama;
desc cse;
select * from cse;
alter table cse 
drop column smarks;
desc cse;

create table it(
sid int,
sname varchar(100),
srno int,
smarks int,
scity varchar(100)
);
desc it;
select * from it;
select* from cse;

insert into it
values(1,"abc",10,90,"Chennai");
insert into it 
values
(2,"def",11,80,"Chennai"),
(3,"ghi",12,85,"mysore"),
(4,"jkl",13,50,"kerala");
select * from it;

insert into cse
values
(1,"xyz",20),
(2,"uvw",21),
(3,"rst",22);
select * from cse;

alter table it
drop column smarks;
select * from it;

alter table it
add column age int default 18;
select * from it;

update it
set scity = "mysore"
where sid = 1;
select * from it;

alter table it
change age sage int;
select * from it;

alter table it 
add column smarks int ;
insert into it (smarks)
values
(80),
(70),
(60),
(90);
DELETE FROM it WHERE id = null;
DELETE FROM it WHERE smarks > 59;
alter table it drop smarks;
alter table it
add column smarks int;
update it
set smarks = 80
where sid = 1;
update it
set smarks = 85
where sid = 2;
update it
set smarks = 70
where sid = 3;
update it
set smarks = 60
where sid = 4;
select * from it;

update it
set scity = "Chennai", smarks = 50
where sid = 3;

start transaction;
insert into cse 
values(4,"abc",23);
commit;
select * from cse;
rollback;
select * from cse;

start transaction;
 insert into cse 
values(5,"def",24);
delete from it where sid = 5;
select * from cse;
delete from cse where sid = 4;
rollback;
commit;

start transaction;
 insert into cse 
values(5,"def",24);
savepoint step1;
insert into cse
values(6,"ghi",25);
rollback to step1;
select * from cse;
commit;


show variables like 'autocommit';
